# Pinned to exact versions used to train the committed models/*.joblib.
# Reproducibility caught a real scikit-learn version-skew warning during
# clone verification (models trained on 1.8.0, an unpinned install grabbed
# 1.9.1) -- pinning prevents that silently recurring for anyone else.
requests==2.31.0
python-dotenv==1.0.1
earthengine-api==1.7.28
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.7.0
statsmodels==0.15.0
joblib==1.5.3
matplotlib==3.10.9
streamlit==1.57.0
